{
  "name": "@biongff/biongff-viewer",
  "private": true,
  "version": "0.1.0",
  "author": "BioNGFF",
  "license": "MIT",
  "description": "A javascipt viewer for BioNGFF.",
  "scripts": {
    "dev": "pnpm --filter viewer dev",
    "lint": "pnpm --filter viewer lint",
    "lint:fix": "pnpm --filter viewer lint:fix",
    "build": "pnpm --filter viewer build --base ./ --outDir ../dist"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
